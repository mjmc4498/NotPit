{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Agenda Template",
  "description": "Schema for an agenda template.",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "description": "The unique identifier for a template."
    },
    "name": {
      "type": "string",
      "description": "The name of the template (e.g., '1:1', 'Daily Standup')."
    },
    "agenda_titles": {
      "type": "array",
      "items": { "type": "string" },
      "description": "An array of strings, where each string is the title of an agenda item."
    }
  },
  "required": ["name", "agenda_titles"]
}
